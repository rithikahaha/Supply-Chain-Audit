# data/processed

Not tracked in git. Generated by `python/prepare_dashboard_data.py` from `data/raw/DataCoSupplyChainDataset.csv`.

`orders_clean.csv` is the PII-stripped, order-level extract that the Tableau dashboard connects to directly for interactive filtering (region, shipping mode, date, customer segment).
